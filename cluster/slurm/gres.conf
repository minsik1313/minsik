# GPU 자원 정의 — Machine A 에만 배치: /etc/slurm/gres.conf
# (B 에는 GPU가 없으므로 이 파일 불필요)
#
# /dev/nvidia0, /dev/nvidia1 두 장의 RTX 5090.
Name=gpu Type=rtx5090 File=/dev/nvidia0
Name=gpu Type=rtx5090 File=/dev/nvidia1
