# 로컬 오디오 멀티모달 파인튜닝 의존성 (Qwen2.5-Omni-3B + ms-swift)
# CUDA 12.x GPU 환경 기준. torch는 환경에 맞는 빌드로 먼저 설치 권장.

# 핵심 프레임워크
ms-swift[all]>=3.0
transformers>=4.49.0
accelerate>=0.34.0
peft>=0.13.0

# QLoRA(4bit) 양자화
bitsandbytes>=0.44.0

# 오디오 디코딩/전처리
librosa>=0.10.0
soundfile>=0.12.1

# Qwen Omni 입력 유틸 (오디오/비디오/이미지 멀티모달 패킹)
qwen-omni-utils

# 평가 지표 (ASR WER 등)
jiwer>=3.0.0

# 데이터 처리
datasets>=3.0.0
